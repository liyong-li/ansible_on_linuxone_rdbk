apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - name: {{ ocp_idp }}
    mappingMethod: claim
    type: HTPasswd
    htpasswd:
      fileData:
        name: {{ ocp_idp_users_secret }} 
